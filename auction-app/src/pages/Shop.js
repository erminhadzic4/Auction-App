import React from "react";

const Shop = () => {
  return (
    <div
      style={{
        minHeight: "586px",
        display: "grid",
        alignItems: "center",
        textAlign: "center",
      }}
    >
      Shop Page Content
    </div>
  );
};

export default Shop;
