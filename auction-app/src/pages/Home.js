import React from "react";

const Home = () => {
  return (
    <div
      style={{
        minHeight: "586px",
        display: "grid",
        alignItems: "center",
        textAlign: "center",
      }}
    >
      Home Page Content
    </div>
  );
};

export default Home;
